const chai = require('chai');
const expect = chai.expect;

// const User = require('../test-user-data');


describe ('User', () => {
  let user1, user2, user3, activity, sleep, hydration;

  beforeEach(() => {
    user1 = new User()
  });

});
